import { test,expect } from "@playwright/test";

test("Campaign Brain demo shows the complete strategic path",async({page})=>{await page.goto("/campaigns");await expect(page.getByRole("heading",{name:"Campañas"})).toBeVisible();await page.getByText("Tu equipo de marketing coordinado",{exact:true}).click();await expect(page.getByText("Why did Spectro create this campaign?")).toBeVisible();await page.getByRole("link",{name:"Research"}).click();await expect(page.getByText("Knowledge-based research")).toBeVisible();await expect(page.getByText("WHAT WE ASSUME")).toBeVisible();await expect(page.getByText("REQUIRES EXTERNAL RESEARCH")).toBeVisible();await page.getByRole("link",{name:"Channels"}).click();await expect(page.getByText("instagram",{exact:true})).toBeVisible();await page.getByRole("link",{name:"Content Plan"}).click();await expect(page.getByText("Distribución editorial")).toBeVisible();await expect(page.getByText("La distribución suma 100%.")).toBeVisible();});

test("a campaign can start from an objective in demo mode",async({page})=>{await page.goto("/campaigns/new");await page.getByLabel("Objetivo").selectOption({index:1});await page.getByRole("button",{name:"Crear campaña"}).click();await expect(page.getByRole("heading",{name:"Tu equipo de marketing coordinado"})).toBeVisible();});
