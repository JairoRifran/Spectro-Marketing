"use client";
import { useState } from "react";

export function TaskComposer({demo}:{demo:boolean}){const[message,setMessage]=useState("");async function submit(formData:FormData){if(demo){setMessage("Tarea demo creada localmente; no se persistió.");return;}const payload=Object.fromEntries(formData);const res=await fetch("/api/tasks",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});setMessage(res.ok?"Tarea creada y enviada al motor.":"No se pudo crear la tarea.");if(res.ok)location.reload()}return <details className="composer"><summary className="primary-button">Crear tarea</summary><form action={submit}><label>Título<input name="title" required minLength={3}/></label><label>Tipo<input name="type" defaultValue="manual.analysis" required/></label><label>Razón<textarea name="reason" required/></label><label>Impacto esperado<textarea name="expected_impact"/></label><div className="form-row"><label>Prioridad<select name="priority" defaultValue="medium"><option>low</option><option>medium</option><option>high</option><option>urgent</option></select></label><label>Riesgo<select name="risk_level" defaultValue="low"><option>low</option><option>medium</option><option>high</option></select></label></div><button className="primary-button">Guardar</button>{message&&<p>{message}</p>}</form></details>}
export function KnowledgeComposer({demo}:{demo:boolean}){const[message,setMessage]=useState("");async function submit(formData:FormData){if(demo){setMessage("Elemento demo creado localmente; no se persistió.");return;}const res=await fetch("/api/knowledge",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(formData))});setMessage(res.ok?"Conocimiento guardado.":"No se pudo guardar.");if(res.ok)location.reload()}return <details className="composer"><summary className="primary-button">Nuevo conocimiento</summary><form action={submit}><label>Título<input name="title" required/></label><label>Tipo<select name="type"><option>company</option><option>product</option><option>persona</option><option>brand</option><option>research</option><option>learning</option><option>policy</option><option>other</option></select></label><label>Contenido<textarea name="content" required/></label><input type="hidden" name="source" value="manual"/><button className="primary-button">Guardar</button>{message&&<p>{message}</p>}</form></details>}
